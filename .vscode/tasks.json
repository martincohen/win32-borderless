{
    "tasks": [
        {
            "type": "process",
            "label": "build",
            "command": "${workspaceRoot}\\build.bat",
            "options": {
                "cwd": "${workspaceRoot}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}